package com.fashi.multiscreenexample;

import androidx.appcompat.app.AppCompatActivity;

import android.os.Bundle;

public class MainActivity extends AppCompatActivity {

    //TODO
    // Create the other activity for the other images to display the tour
    // Change the Next Button to use a arrow image
    // Add Functonality to the app to move to next activity
    // Make the app a little bit fine.

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_main);
    }
}
